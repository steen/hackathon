{
  "name": "hackathon",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --if-present run dev",
    "build": "pnpm -r --if-present run build",
    "smoke": "bash scripts/smoke.sh",
    "test": "bash scripts/smoke.sh && pnpm -r --if-present run test"
  }
}
