- **server**: add a one-line forward-reference comment in `apps/server/main.go`
  pointing at `boot.go`, where `openAndMigrate` and `requireSecret` are
  defined. Cosmetic only; no behavior change. Closes #813.
