### Changed

- `apps/web/src/routes/Chat.tsx` is wiring-only: hooks → memos → JSX composing TopBar/Sidebar/ChannelHeader/MessageList/MessageComposer. JSX indentation reformatted after the TopBar wrap.
