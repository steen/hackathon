### Added

- Phase 10 wire types in `packages/api-client/src/types.ts` and `packages/go-client/{auth,users,channels,messages,dms,ws}.go`: `MessageEnvelope` (Phase-10 encrypted-message envelope per decision-log L21), `WrapEntry` (per-recipient root-key wrap per L5 + §7), `MembershipBlock` (inviter-signed membership row per §10 + L22). `User` gains optional `box_pubkey` / `sign_pubkey` (L2); `Channel` gains optional `is_public` (L24); `Message` and `DMMessage` gain optional `envelope` (L21 + L26 optional-first). `ChannelEventKind` extends to `members_changed` / `key_received` / `wrap_failed` (L9 + L29) with the matching discriminated-union shapes on both sides. `tests/e2e/phase-10/types.test.ts` lands the L10 cross-language drift assertion. Server population follows in #4..#9; `Message.envelope` is narrowed to required in #983 (L26).
