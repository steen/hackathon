### Docs

- `packages/go-client/{auth,channels,messages,ws,client}.go` and `packages/api-client/src/types.ts` now carry top-of-file `sync with <counterpart>` comments. `CLAUDE.md` gains a short "Wire types" section describing the cross-file contract (mirror both sides, add an e2e assertion; codegen out of scope at five envelope types). Comments only — no behavior change.
