### Fixed

- `specs/PRD.md` §10: drop the bogus `wraps-needed bucket` suffix on the `POST /api/channels/{id}/keys` 429 row — that bucket name does not exist in the Phase 10 contract (the GET bucket is `wraps-needed-read` per decision-log L36; the recovery bucket is `replay-wrap` per L35; the keys-RPC POST has no dedicated bucket and shares the existing per-user write rate limit). The row now ends bare `429`, matching the `POST /api/channels/{id}/members` row in the same table. Docs-only; no runtime change.
