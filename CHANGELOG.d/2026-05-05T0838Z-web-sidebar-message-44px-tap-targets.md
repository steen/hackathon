### Changed

- On phone-width viewports (≤767px), the sidebar Sign-out button and the message-list Load-older + Retry buttons now meet the 44px minimum tap target from iOS HIG / WCAG 2.5.5. Presence-list rows are non-interactive text and do not require a tap target. Desktop layout (≥768px) is unchanged. (#631)
