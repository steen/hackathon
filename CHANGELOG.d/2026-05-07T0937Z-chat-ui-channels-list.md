### Changed

- `ChannelsList` extracted from `apps/web/src/routes/Chat.tsx` into `@hackathon/chat-ui`. Preserves `aria-label="Channels"` and `aria-current="true"` on the active row; `Channel` type re-exported.
