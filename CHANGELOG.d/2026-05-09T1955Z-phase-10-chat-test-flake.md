### Fixed

- `apps/web/src/routes/Chat.test.tsx`: deflake `test_web_chat_does_not_flash_empty_channel_hint_before_history_resolves`. The previous assertion took a single-frame `queryByTestId` snapshot right after `findByRole("heading")`, which raced React's commit ordering — the render with `activeChannel="C1"` commits before `useMessages`'s `setHistoryLoading(true)` re-render lands, opening a brief window where all four `showEmptyChannelHint` conditions hold. The test now attaches a `MutationObserver` across the whole in-flight window and waits for `listMessages` to have been called plus a microtask + macrotask flush, so the assertion reflects the gate's invariant ("hint never appears while listMessages is pending") rather than one DOM frame. 50/50 deterministic locally.
