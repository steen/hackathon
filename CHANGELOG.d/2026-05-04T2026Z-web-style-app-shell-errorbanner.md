### Added

- Web: visual styling for `<ErrorBanner />` in `apps/web/src/styles.css`. The component shipped in #157 with `role=alert` and class names but no matching CSS, rendering as unstyled chrome; this gives it conspicuous color/spacing using the existing `--error` token. (#492)
