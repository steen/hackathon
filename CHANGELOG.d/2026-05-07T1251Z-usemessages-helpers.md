### Changed

- `apps/web` (`useMessages` hook): split the 429-LOC hook into three files. Pure reconciliation math (`findPendingMatch`, `reconcileWsMessage`, `mergeFetchedCatchup`, `reconcilePersisted`, `markPendingFailed`, `startRetry`, `oldestCommittedId`, `prependOlderPage`, `makePendingRow`, `newPendingId`) lives in `useMessages.helpers.ts` with a unit suite (`useMessages.helpers.test.ts`, 34 cases) covering body-window matching, FIFO disambiguation, REST/WS reconcile, retry flow, and error classification. The impure channel bootstrap (history fetch + WebSocket wiring + reconnect-catchup) moves to `useMessages.connect.ts`. `useMessages.ts` shrinks to state wiring + four short callbacks (~196 LOC). Behavior unchanged: existing `useMessages.test.ts`, `Chat.test.tsx`, and the chat Playwright spec stay green without modification. Closes #807. (2026-05-07T12:51Z)
