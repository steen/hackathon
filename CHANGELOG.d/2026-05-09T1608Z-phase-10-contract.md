### Added

- `specs/plans/phase-10/`: Phase 10 contract documents for the E2E encryption epic (#975 / #976). `README.md` carries the wave table (10 waves across 13 sub-issues) and a Phase 9 surface-verification matrix at branch-cut commit `c8ff561`. `encryption.md` pins the `(box_pubkey, sign_pubkey)` identity pair, the Argon2id (`t=3, m=64 MiB, p=1`) + HKDF-SHA256 derivation pipeline, the L37 `^[a-z0-9_-]{3,32}$` username regex, the three signature-scope prefixes (`snakd-msg-v1:channel:`, `snakd-msg-v1:dm:`, `snakd-mship-v1:`), the `cipher_suite = 0x01 = naclbox-v1` byte, and the `WrapEntry` / `MembershipBlock` / `Envelope` wire types. `membership.md` pins the `channel_members` schema (with §10 inviter-signature columns + L33 partial index for NULL-sig forensic scans), the `is_public` migration and `#general` auto-add rule, the `POST /api/channels/{id}/members` body shape with self-bootstrap carve-out, and the `MaterializeChannelReadsTx` member-filter (L25). `keys.md` pins the `channel_keys` / `dm_conversation_keys` schemas (DM has no `generation_id` — DMs never rotate per L6), the three modes of `POST /api/channels/{id}/keys` (bootstrap when `max IS NULL`, fill-in when `gen == max`, rotation when `gen == max+1`), the `wraps-needed` response with embedded `MembershipBlock` per row (L22), and the `replay-wrap` recovery path with its 3-failures-in-1h cool-down (L29 + L35). `security.md` documents the option-C threat model (operator-with-full-control + passive network attacker), R1.1 modified-client residual, R1.2 operator-as-public-channel-member residual, accepted metadata exposure (L19), TOFU + key-change warning UX (L20 + L34 split rules), wipe-and-reset migration with boot guard (L18), and the L37 lowercase-username CRYPTO-1 defense.
- `specs/PRD.md`: §4 In-Scope adds E2E encryption + public/private channels with explicit membership; §4 Out-of-Scope drops "E2E encryption" (now shipped); §9 threat-model rewrites the trust-boundary text to describe the post-Phase-10 wrap-list source-of-truth (`channel_members` relation), names R1.1 + R1.2 as accepted residuals, and updates the "out of MVP scope" list to drop E2E + add the L20/L16 carve-outs; §10 documents the new auth-register/login pubkey fields, the Phase 10 channel-membership endpoints (`POST/GET /api/channels/{id}/members`, `DELETE /api/channels/{id}/members/{user_id}` with L8 `#general` immutability and L16 member-removal-only rotation trigger), the keys-RPC + `wraps-needed` + `replay-wrap` endpoints with rate-limit buckets, and the WS `channel` event extensions (`members_changed`, `key_received`, `wrap_failed`); §11 adds the Phase 10 schema block (the `users.box_pubkey` / `users.sign_pubkey` + `channels.is_public` columns, the `channel_members` / `channel_keys` / `dm_conversation_keys` tables, and the seven-column envelope rewrite of `messages` + `dm_messages` per L23); §13 Shipped post-MVP lists E2E encryption + public/private channels as shipped Phase 10 with cross-links to all four `specs/plans/phase-10/*.md` files.

No runtime code changes. The decision log at `lt show -p e2e-encryption 3` (numbered §1–§10, locked-in defaults L1–L41, residual risks R1.1 + R1.2) is the authoritative source; the documents added here are the human-readable reference all 12 downstream Phase 10 sub-issues cite.
