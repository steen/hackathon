- fix(e2e): assert WS-drops reconnect via api-client transition log instead of polling the DOM badge — the badge collapsed fast transients on a quick reconnect, making `tests/e2e/playwright/web.spec.ts` "WS drops + restores" flaky (#110). `WebSocketClient` now emits a `transition` event (`connecting | open | closed`) and exposes a static `observe()` for cross-instance recording; the web app records transitions on `window.__chatd.wsTransitions` for the e2e harness.
