### Web

- Messages list is now an aria-live log region (`role="log"`, `aria-live="polite"`, `aria-relevant="additions"`, `aria-atomic="false"`, `aria-label="conversation"`). Screen readers now announce each newly arriving message individually instead of staying silent on auto-scroll. (#454)
