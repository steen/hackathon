### Added

- Web: phone-width responsive baseline. Adds a `@media (max-width: 767px)` breakpoint to `apps/web/src/styles.css`, collapsing the chat layout to a single column with the sidebar stacked above the messages region; channel-list buttons get a 44px minimum tap target. Desktop (≥768px) is unchanged. Drawer toggle, iOS soft-keyboard handling, and remaining tap-target bumps land in follow-ups under #63. (#612)
