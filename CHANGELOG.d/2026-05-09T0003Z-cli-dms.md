- feat(cli): add `chatd dm list/send/history/read/watch` and `chatd channels read <name> <message-id>`. The DM tree resolves a peer by username (via cached `/api/users`) or ULID, then drives the Phase 9 client surface; `dm watch` filters `{type:"dm"}` frames to one conversation when given a peer. Output format mirrors `chatd channels list` (tab-separated) so existing CLI scripts compose. (#875)
