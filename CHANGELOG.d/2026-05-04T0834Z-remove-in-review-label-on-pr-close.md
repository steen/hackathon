### Internal

- **ci**: add `.github/workflows/remove-in-review-label.yml` to strip the `in-review` label from PRs when they close (merged or not), so the label no longer lingers on closed PRs. (2026-05-04T08:34Z)
